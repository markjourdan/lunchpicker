@model LunchPicker.Domain.Entities.State
   

@(
    Html.Kendo().DropDownListFor(m => m)
    .Name("State")
    .DataTextField("FullName")
    .DataValueField("StateId")
    .DataSource(data => data.Read("GetStates", "Restaurant", new {Area = "Clique"})).ToClientTemplate()
    )