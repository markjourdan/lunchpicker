@using LunchPicker.Web.Areas.Clique.Models.Users
@model dynamic

@{
    ViewBag.Title = "Manage Users";
}

<h1>@ViewBag.Title</h1>

@(Html.Kendo()
    .Grid<UserModel>()
    .Name("Users")
    .Columns(columns =>
            {
                columns.Bound(p => p.UserName);
                columns.Bound(p => p.FirstName).Width(100);
                columns.Bound(p => p.LastName).Width(100);
                columns.Bound(p => p.EmailAddress).Width(140);
                columns.Command(command => { command.Edit(); command.Destroy(); }).Width(200);
            })
    .DataSource(data => data.Ajax()
        .PageSize(20)
        .ServerOperation(false)
        .Model(model =>
            {
                model.Id(p => p.UserId);
                model.Field(column => column.UserId).Editable(false);
            })
        .Events(events => events.Error("error_handler"))
        .Create(create => create.Action("AddUser", "Users", new {Area = "Clique"}).Type(HttpVerbs.Put))
        .Read(read => read.Action("GetUsers", "Users", new { Area ="Clique"}).Type(HttpVerbs.Get))
        .Update(update => update.Action("UpdateUser", "Users", new { Area = "Clique"}).Type(HttpVerbs.Post))
        .Destroy(destroy => destroy.Action("DeleteUser", "Users", new {Area = "Clique"}).Type(HttpVerbs.Delete))
        )
    .ToolBar(toolbar => toolbar.Create().Text("new user"))
    .Editable(editable => editable.Mode(GridEditMode.InLine))
    .Sortable()
    .Selectable()
    .Pageable(p => p.PageSizes(true).Refresh(true))
    .Scrollable(s => s.Height(500))
    )


@section scripts
{
    <script type="text/javascript">
        function error_handler(e) {
            if (e.errors) {
                var message = "Errors:\n";
                $.each(e.errors, function(key, value) {
                    if ('errors' in value) {
                        $.each(value.errors, function() {
                            message += this + "\n";
                        });
                    }
                });
                alert(message);
            }
        }
    </script>
}

